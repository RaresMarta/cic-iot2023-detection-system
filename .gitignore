.venv/
.claude/
/data/j
memory/
wandb/

__pycache__/
*.pyc
.ipynb_checkpoints/

# LaTeX build artefacts
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.lof
*.log
*.lot
*.out
*.synctex.gz
*.toc
# Beamer navigation/verbatim artefacts (regenerated by pdflatex)
*.nav
*.snm
*.vrb

# data
*.parquet

# Reproducibility-script build outputs (regenerated by regenerate_results.py / make_report_figures.py)
_results_full.joblib
_results_summary.json
_paper_numbers.json
models_backup_39feat/

# Upload bundles (data/code shipped to Colab/Kaggle for training)
*.zip

# Trained model artifacts — large (RF forests up to ~1.4 GB) and regenerable by
# `python -m ids.training`; kept out of git and delivered separately.
/models/
# HF Space deploy artifact — the RF forest exceeds GitHub's 100 MB limit and is
# uploaded to the Space directly (see deploy/hf-space/DEPLOY.md), not tracked here.
/deploy/hf-space/models/ids_rf_random_2class.joblib

# Benign-augmentation A/B model artifacts — same large/regenerable class as /models/
# (the 2-class RF alone exceeds GitHub's 100 MB limit); kept out of git.
/models_aug/

# Audited literature — copyrighted paper PDFs kept local only; see docs/literature/README.md
docs/literature/*.pdf

# Python packaging build artifacts
*.egg-info/

# macOS
**/.DS_Store
